<section class="panel conversation-shell" style="@PanelStyle">
    <div class="conversation-header">
        <div>
            <p class="section-kicker">Realtime chat</p>
            <h3>@Title</h3>
            @if (!string.IsNullOrWhiteSpace(Subtitle))
            {
                <p>@Subtitle</p>
            }
        </div>
        <span class="conversation-status">Đang mở</span>
    </div>

    <div class="message-list">
        @if (Messages.Count == 0)
        {
            <p class="conversation-empty">@EmptyText</p>
        }
        else
        {
            @foreach (var message in Messages)
            {
                <article class="message-bubble @(message.IsMine ? "mine" : "theirs")">
                    <p>@message.Content</p>
                    <small>@message.CreatedDate.ToLocalTime().ToString("dd/MM HH:mm")</small>
                </article>
            }
        }
    </div>

    <div class="send-row">
        <InputText @bind-Value="draftMessage" class="form-control" placeholder="@Placeholder" />
        <button class="btn btn-primary conversation-send" @onclick="SendAsync">
            <AppIcon Name="send" />
            <span>Gửi</span>
        </button>
    </div>

    @if (!string.IsNullOrWhiteSpace(ErrorMessage))
    {
        <div class="conversation-error text-danger small">@ErrorMessage</div>
    }
</section>

@code {
    [Parameter, EditorRequired]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string? Subtitle { get; set; }

    [Parameter, EditorRequired]
    public IReadOnlyList<MessageDto> Messages { get; set; } = Array.Empty<MessageDto>();

    [Parameter]
    public string EmptyText { get; set; } = "Chưa có tin nhắn.";

    [Parameter]
    public string Placeholder { get; set; } = "Nhập tin nhắn";

    [Parameter]
    public string? ErrorMessage { get; set; }

    [Parameter]
    public EventCallback<string> OnSend { get; set; }

    [Parameter]
    public string PanelHeight { get; set; } = "clamp(420px, 68vh, 760px)";

    private string? draftMessage;

    private string PanelStyle => $"--conversation-panel-height: {PanelHeight};";

    private async Task SendAsync()
    {
        if (string.IsNullOrWhiteSpace(draftMessage))
        {
            return;
        }

        var message = draftMessage;
        draftMessage = string.Empty;
        await OnSend.InvokeAsync(message);
    }
}

<style>
    .conversation-shell {
        display: grid;
        grid-template-rows: auto minmax(0, 1fr) auto auto;
        gap: 0;
        height: var(--conversation-panel-height, clamp(420px, 68vh, 760px));
        padding: 0;
        overflow: hidden;
    }

    .conversation-header,
    .send-row,
    .conversation-error {
        padding-inline: 20px;
    }

    .conversation-header {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        justify-content: space-between;
        align-items: center;
        padding-block: 18px;
        border-bottom: 1px solid var(--border);
        flex: 0 0 auto;
    }

    .conversation-header p {
        margin: 4px 0 0;
        color: var(--text-muted);
        font-size: 13px;
    }

    .conversation-status {
        display: inline-flex;
        align-items: center;
        min-height: 28px;
        padding: 0 12px;
        border-radius: 999px;
        background: var(--app-highlight-soft);
        color: var(--success);
        font-size: 12px;
        font-weight: 800;
    }

    .message-list {
        min-height: 0;
        display: grid;
        align-content: start;
        gap: 10px;
        overflow-y: auto;
        padding: 20px;
        background: var(--bg-muted);
    }

    .conversation-empty {
        margin: 0;
        color: var(--text-muted);
        font-size: 13px;
    }

    .message-bubble {
        max-width: 72%;
        padding: 12px 14px;
        border-radius: 18px;
        box-shadow: 0 10px 18px rgba(15, 23, 42, 0.04);
    }

    .message-bubble.mine {
        justify-self: end;
        background: linear-gradient(135deg, var(--accent) 0%, #ff8aa8 100%);
        color: #fff;
        border-top-right-radius: 8px;
    }

    .message-bubble.theirs {
        justify-self: start;
        background: var(--bg-shell);
        color: var(--text-primary);
        border: 1px solid var(--border);
        border-top-left-radius: 8px;
    }

    .message-bubble p,
    .message-bubble small {
        margin: 0;
    }

    .message-bubble p {
        white-space: pre-wrap;
        line-height: 1.5;
        font-size: 15px;
    }

    .message-bubble small {
        display: block;
        margin-top: 4px;
        opacity: 0.82;
        font-size: 12px;
    }

    .send-row {
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto;
        gap: 10px;
        align-items: stretch;
        padding-block: 18px;
        border-top: 1px solid var(--border);
        background: var(--bg-shell);
        flex: 0 0 auto;
    }

    .conversation-send {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        min-width: 108px;
        justify-content: center;
    }

    .conversation-error {
        padding-top: 0;
        padding-bottom: 18px;
    }
</style>
